# - Config file for the DOTPRODUCT package
# It defines the following variables
#  DOTPRODUCT_INCLUDE_DIRS - include directories for DotProduct library
#  DOTPRODUCT_LIBRARIES    - libraries to link against

# Compute paths
get_filename_component(DOTPRODUCT_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(DOTPRODUCT_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

# These are IMPORTED targets created by DotProductConfig.cmake
set(DOTPRODUCT_LIBRARIES "@CONF_LIB_DIRS@")
